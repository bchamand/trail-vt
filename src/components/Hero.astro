---
---
<section id="top" class="relative overflow-hidden" style="min-height: 88vh;">
  <!-- Parallax image -->
  <img
    id="hero-img"
    src="/images/garonne.jpg"
    alt=""
    class="absolute inset-0 w-full object-cover pointer-events-none"
    style="height: 110%; object-position: center 40%; filter: brightness(0.7) contrast(1.1) saturate(0.85); transform: translateY(0) scale(1.05); will-change: transform;"
  />

  <!-- Bottom gradient fade -->
  <div class="absolute inset-0 pointer-events-none"
       style="background: linear-gradient(to bottom, rgba(15,20,18,0.55) 0%, transparent 30%, transparent 55%, rgba(15,20,18,0.95) 100%);"></div>

  <!-- Grain -->
  <div class="grain"></div>

  <!-- Vertical side rails (desktop) -->
  <div class="hidden lg:block absolute left-7 top-1/2 -translate-y-1/2 side-rail">CAPITALE VOLQUE — 2300 ANS</div>
  <div class="hidden lg:block absolute right-7 top-1/2 -translate-y-1/2 side-rail">III · X · MMXXVI</div>

  <!-- Content -->
  <div class="absolute inset-0 flex flex-col justify-end px-5 pb-10 sm:px-10 sm:pb-16 lg:px-20 lg:pb-16">
    <p class="mono-label mb-4 sm:mb-6" style="line-height: 1.5;">
      <span class="hidden sm:inline">— SAMEDI 3 OCTOBRE 2026 · COUCHER DU SOLEIL 19H32</span>
      <span class="sm:hidden">— SAM. 3 OCT. 2026 · 15H45</span>
    </p>

    <h1 class="hero-title">
      Sur la trace<br/>
      des <em style="color: var(--color-accent); font-style: italic;">Volques<br/>Tectosages.</em>
    </h1>

    <div class="mt-7 sm:mt-12 flex flex-col sm:flex-row sm:items-end sm:justify-between gap-5">
      <p class="max-w-[460px] text-[14px] sm:text-[16px] opacity-85 leading-[1.5] m-0" style="text-wrap: pretty;">
        Vieille-Toulouse fut la capitale des Volques Tectosages il y a 2300 ans. Cinq épreuves, une forêt protégée, un territoire à reconquérir avant la nuit.
      </p>
      <!-- Mobile CTA -->
      <div class="sm:hidden w-full">
        <a href="https://chrono-start.com/" target="_blank" rel="noopener noreferrer"
           class="btn-gold w-full" style="padding: 16px 28px;">PRENDRE SON DOSSARD →</a>
      </div>
      <!-- Desktop scroll indicator -->
      <div class="hidden sm:flex items-center gap-3">
        <span class="text-[11px] tracking-[0.2em] opacity-70" style="font-family: var(--font-mono);">SCROLL</span>
        <div style="width: 1px; height: 32px; background: rgba(232,227,211,0.4);"></div>
      </div>
    </div>
  </div>
</section>

<script>
  const img = document.getElementById('hero-img') as HTMLImageElement;
  let ticking = false;
  function update() {
    const y = window.scrollY;
    img.style.transform = `translateY(${y * 0.3}px) scale(1.05)`;
    ticking = false;
  }
  window.addEventListener('scroll', () => {
    if (!ticking) {
      requestAnimationFrame(update);
      ticking = true;
    }
  }, { passive: true });
</script>
